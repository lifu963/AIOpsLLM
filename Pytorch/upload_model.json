{
    "name": "upload_model",
    "description": "Upload model parameters from the local environment to the cloud.",
    "parameters": {
        "type": "object",
        "properties": {
            "local_path": {
                "type": "string",
                "description": "Path to the model parameters stored locally. Required."
            },
            "cloud_path": {
                "type": "string",
                "description": "Path where the model parameters will be stored in the cloud. Required."
            }
        },
        "required": ["local_path", "cloud_path"]
    }
}
